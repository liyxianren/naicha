# 🎮 游戏主界面开发计划

## 📋 项目现状

### ✅ 已完成
- 后端API：9组API，40+端点全部完成
- 游戏大厅：创建/加入房间功能
- 房间准备：玩家准备、自动开始、状态同步
- 基础框架：React + TypeScript + Ant Design + Zustand

### 🎯 目标
开发完整的游戏主界面，实现10回合的回合制游戏流程

---

## 📐 游戏主界面架构设计

### 界面布局
```
┌─────────────────────────────────────────────────────────┐
│  顶部信息栏                                                │
│  [游戏名称] [当前回合 X/10] [玩家资金 ¥10000]              │
└─────────────────────────────────────────────────────────┘
┌──────────────┬──────────────────────────────────────────┐
│              │                                          │
│  玩家列表     │         决策面板（Tabs切换）               │
│              │                                          │
│  玩家1 💰    │  Tab1: 店铺决策 🏠                        │
│  玩家2 💰    │  Tab2: 员工管理 👥                        │
│  玩家3 💰    │  Tab3: 市场行动 📢                        │
│  玩家4 💰    │  Tab4: 产品研发 🧪                        │
│              │  Tab5: 生产计划 🧋                        │
│  当前回合    │                                          │
│  轮到：玩家X  │  [提交决策按钮]                           │
│              │                                          │
└──────────────┴──────────────────────────────────────────┘
```

---

## 🔧 开发阶段划分

### **阶段1：游戏主框架** [优先级: 🔴 最高]
**目标**：搭建游戏主界面的基础框架和回合控制

#### 任务清单
1. **创建游戏主组件结构**
   - 顶部信息栏（游戏名、回合数、玩家资金）
   - 左侧玩家列表（显示所有玩家状态）
   - 右侧决策面板（Tab切换容器）
   - 底部操作按钮（提交决策、查看报表）

2. **实现回合控制逻辑**
   - 轮询检查当前回合状态
   - 判断是否轮到当前玩家
   - 显示"等待其他玩家"状态
   - 回合推进机制

3. **创建状态管理**
   - 扩展 `decisionStore.ts` 添加回合相关状态
   - 当前回合数据
   - 玩家决策状态
   - 提交锁定状态

**预计时间**: 2-3小时

---

### **阶段2：店铺决策模块** [优先级: 🔴 最高]
**目标**：实现第1回合的开店决策

#### 任务清单
1. **开店界面**
   - 输入店铺地点（可选）
   - 输入租金（数字输入框）
   - 选择装修等级（0/1/2/3）
   - 显示装修费用和员工上限
   - 提交开店决策

2. **API集成**
   - 调用 `POST /shops/open`
   - 调用 `POST /shops/{player_id}/upgrade`
   - 获取装修费用 `GET /shops/decoration-costs`

3. **校验逻辑**
   - 检查资金是否足够
   - 确保只能在第1回合开店
   - 已开店后显示店铺信息

**预计时间**: 1-2小时

---

### **阶段3：员工管理模块** [优先级: 🟡 高]
**目标**：实现员工招聘和管理

#### 任务清单
1. **员工列表展示**
   - 显示当前员工列表
   - 显示每个员工的工资和生产力
   - 显示总生产力统计

2. **招聘界面**
   - 输入员工姓名
   - 输入工资（建议范围提示）
   - 输入生产力（1-10）
   - 检查员工上限（根据装修等级）

3. **解雇功能**
   - 员工列表每项添加"解雇"按钮
   - 确认对话框
   - 解雇后更新列表

4. **API集成**
   - `POST /employees/hire`
   - `POST /employees/{id}/fire`
   - `GET /employees/player/{player_id}`
   - `GET /employees/player/{player_id}/productivity`

**预计时间**: 2-3小时

---

### **阶段4：产品研发模块** [优先级: 🟡 高]
**目标**：实现掷骰子研发产品

#### 任务清单
1. **产品配方展示**
   - 显示所有7种产品配方
   - 显示每个产品的：
     - 配方原料
     - 难度等级（1/2/3）
     - 研发成功率（≥2/3/4）
     - 研发费用（600元）
     - 圈粉率（5%/20%/30%）
   - 区分已解锁/未解锁

2. **研发界面**
   - 选择要研发的产品
   - 点击"研发"按钮
   - 动画展示掷骰子过程（1-6）
   - 显示成功/失败结果
   - 失败时仍然扣费

3. **骰子动画**
   - CSS动画展示骰子翻滚
   - 1秒后显示结果
   - 成功：绿色提示 + 解锁产品
   - 失败：红色提示

4. **API集成**
   - `POST /products/research`
   - `GET /products/recipes?player_id={id}`
   - `GET /products/player/{player_id}/unlocked`
   - `GET /products/player/{player_id}/research-history`

**预计时间**: 2-3小时

---

### **阶段5：市场行动模块** [优先级: 🟡 高]
**目标**：实现广告和市场调研

#### 任务清单
1. **广告投放**
   - 显示已解锁产品列表
   - 选择要投放广告的产品
   - 点击"投放广告"（800元）
   - 掷骰子动画（1-6）
   - 显示获得的广告分数
   - 更新产品的广告分

2. **市场调研**
   - 点击"市场调研"按钮（500元）
   - 查看当前回合的客流量
   - 显示高购买力顾客数量
   - 显示低购买力顾客数量

3. **行动历史**
   - 显示本回合的市场行动记录
   - 显示花费金额

4. **API集成**
   - `POST /market/advertisement`
   - `POST /market/research`
   - `GET /market/costs`
   - `GET /market/actions/{player_id}`

**预计时间**: 2小时

---

### **阶段6：生产计划模块** [优先级: 🔴 最高]
**目标**：实现产品生产力分配和定价

#### 任务清单
1. **生产计划表单**
   - 显示所有已解锁产品
   - 每个产品显示：
     - 当前定价（10-40元，5的倍数）
     - 生产力分配滑块（0 - 总生产力）
     - 原料成本预览
     - 当前广告分
     - 当前口碑分

2. **生产力分配**
   - 总生产力 = 所有员工生产力之和
   - 实时显示剩余可分配生产力
   - 超出时红色警告
   - 拖动滑块分配生产力

3. **定价控制**
   - 输入框：10-40元，步长5
   - 检查定价锁定（每3回合）
   - 锁定期间禁用输入并提示

4. **成本预览**
   - 实时计算原料成本
   - 显示批量折扣
   - 显示预计利润

5. **提交生产计划**
   - 校验所有输入
   - 确认对话框
   - 提交到后端

6. **API集成**
   - `POST /production/submit`
   - `POST /production/preview-cost`
   - `GET /production/player/{player_id}/round/{round_number}`

**预计时间**: 3-4小时

---

### **阶段7：回合结算与报表** [优先级: 🟡 高]
**目标**：显示回合结算结果和财务报表

#### 任务清单
1. **等待所有玩家**
   - 显示"等待其他玩家提交决策"
   - 实时显示已提交玩家数量
   - 轮询检查回合状态

2. **回合结算页面**
   - 显示销售结果：
     - 每个产品卖出数量
     - 销售收入
   - 显示支出明细：
     - 租金
     - 员工工资
     - 原料成本
     - 市场费用
   - 显示利润
   - 显示新的现金余额

3. **排行榜**
   - 显示所有玩家
   - 按利润排序
   - 显示现金、利润

4. **回合推进**
   - 点击"下一回合"按钮
   - 所有玩家确认后进入下一回合

5. **API集成**
   - `GET /rounds/{game_id}/summary?round_number={n}`
   - `POST /rounds/{game_id}/advance`
   - `GET /finance/player/{player_id}`
   - `GET /finance/game/{game_id}/ranking`

**预计时间**: 2-3小时

---

### **阶段8：游戏结束与总结** [优先级: 🟢 中]
**目标**：显示游戏结束画面和最终排名

#### 任务清单
1. **检测游戏结束**
   - 第10回合结算后检测
   - 自动跳转到结束页面

2. **最终排名页面**
   - 显示最终排名（按总资金）
   - 显示每个玩家的：
     - 最终资金
     - 总利润
     - 总销售额
   - 冠军特效动画

3. **游戏统计**
   - 最佳单回合利润
   - 最畅销产品
   - 研发成功率

4. **操作按钮**
   - 查看详细报表
   - 返回大厅

**预计时间**: 2小时

---

### **阶段9：UI/UX优化** [优先级: 🟢 低]
**目标**：优化界面和用户体验

#### 任务清单
1. **动画效果**
   - 骰子滚动动画
   - 金币增减动画
   - 页面切换过渡
   - 按钮悬浮效果

2. **响应式设计**
   - 适配不同屏幕尺寸
   - 移动端优化

3. **加载状态**
   - API请求时显示loading
   - 骨架屏占位

4. **错误处理**
   - 友好的错误提示
   - 网络断开重连

**预计时间**: 2-3小时

---

## 📅 开发时间表

| 阶段 | 任务 | 预计时间 | 优先级 |
|------|------|----------|--------|
| 1 | 游戏主框架 | 2-3小时 | 🔴 |
| 2 | 店铺决策 | 1-2小时 | 🔴 |
| 3 | 员工管理 | 2-3小时 | 🟡 |
| 4 | 产品研发 | 2-3小时 | 🟡 |
| 5 | 市场行动 | 2小时 | 🟡 |
| 6 | 生产计划 | 3-4小时 | 🔴 |
| 7 | 回合结算 | 2-3小时 | 🟡 |
| 8 | 游戏结束 | 2小时 | 🟢 |
| 9 | UI优化 | 2-3小时 | 🟢 |
| **总计** | **全部功能** | **18-26小时** | - |

---

## 🎨 设计规范

### 颜色主题（延续现有风格）
- 主色：奶茶粉 `#FFB6C1`
- 辅色：奶茶棕 `#8B6F47`
- 金钱色：金色 `#FFD700`
- 成功色：绿色 `#98D982`
- 失败色：红色 `#FF6B6B`

### 组件规范
- 圆角：16px-24px
- 阴影：`0 4px 16px rgba(0, 0, 0, 0.15)`
- 动画：300ms ease-in-out
- 字体：16px-18px 正文，24px-32px 标题

### 图标使用
- 🏠 店铺
- 👥 员工
- 📢 广告
- 🧪 研发
- 🧋 生产
- 💰 金钱
- 🎲 骰子
- 📊 报表

---

## 🔑 关键技术点

### 1. 回合同步机制
```typescript
// 每2秒轮询检查回合状态
useEffect(() => {
  const checkRound = async () => {
    const game = await gameApi.getGame(gameId);
    if (game.current_round !== localRound) {
      // 回合变化，刷新数据
      loadRoundData();
    }
  };

  const interval = setInterval(checkRound, 2000);
  return () => clearInterval(interval);
}, []);
```

### 2. 生产力分配控制
```typescript
const [allocations, setAllocations] = useState<Record<number, number>>({});
const totalProductivity = employees.reduce((sum, e) => sum + e.productivity, 0);
const usedProductivity = Object.values(allocations).reduce((sum, v) => sum + v, 0);
const remainingProductivity = totalProductivity - usedProductivity;
```

### 3. 定价锁定检查
```typescript
const canChangePrice = (product: PlayerProduct, currentRound: number) => {
  if (!product.last_price_change_round) return true;
  return currentRound - product.last_price_change_round >= 3;
};
```

### 4. 骰子动画
```css
@keyframes rollDice {
  0%, 100% { transform: rotate(0deg); }
  25% { transform: rotate(90deg); }
  50% { transform: rotate(180deg); }
  75% { transform: rotate(270deg); }
}

.dice-rolling {
  animation: rollDice 1s ease-in-out;
}
```

---

## 📦 需要创建的文件

### 组件文件
```
frontend/src/pages/
  └─ Game.tsx (已存在，需重写)

frontend/src/components/game/
  ├─ GameHeader.tsx          # 顶部信息栏
  ├─ PlayerList.tsx          # 玩家列表
  ├─ DecisionPanel.tsx       # 决策面板容器
  ├─ ShopDecision.tsx        # 店铺决策
  ├─ EmployeeManagement.tsx  # 员工管理
  ├─ ProductResearch.tsx     # 产品研发
  ├─ MarketAction.tsx        # 市场行动
  ├─ ProductionPlan.tsx      # 生产计划
  ├─ RoundSummary.tsx        # 回合结算
  ├─ DiceRoller.tsx          # 骰子动画组件
  └─ GameEnd.tsx             # 游戏结束页面
```

### Store文件
```
frontend/src/stores/
  └─ gameRoundStore.ts       # 回合状态管理
```

---

## ✅ 验收标准

### 功能验收
- [ ] 能够创建游戏并进入游戏主界面
- [ ] 第1回合能够开店并选择装修
- [ ] 能够招聘员工并管理
- [ ] 能够研发产品（成功率正确）
- [ ] 能够投放广告和市场调研
- [ ] 能够分配生产力和定价
- [ ] 提交决策后等待其他玩家
- [ ] 回合结算正确显示
- [ ] 10回合后游戏结束并显示排名
- [ ] 所有API调用正常
- [ ] 数据持久化正确

### 性能验收
- [ ] 页面加载时间 < 2秒
- [ ] API响应时间 < 500ms
- [ ] 动画流畅60fps
- [ ] 无内存泄漏

### UI验收
- [ ] 界面美观，符合奶茶主题
- [ ] 响应式布局正常
- [ ] 按钮悬浮效果正确
- [ ] 错误提示友好
- [ ] 加载状态清晰

---

## 🚀 开发建议

### 开发顺序
1. **先实现核心流程**：阶段1→2→6（框架+开店+生产）
2. **再补充功能**：阶段3→4→5（员工+研发+市场）
3. **最后完善体验**：阶段7→8→9（结算+结束+优化）

### 测试策略
1. **单模块测试**：每完成一个模块立即测试
2. **集成测试**：完成阶段6后进行完整流程测试
3. **多人测试**：用两个浏览器模拟多人游戏

### 注意事项
⚠️ **定价锁定**：严格检查3回合限制
⚠️ **生产力校验**：确保不超出总生产力
⚠️ **资金检查**：所有操作前检查资金充足
⚠️ **回合同步**：保证多个玩家的回合状态一致
⚠️ **错误处理**：所有API调用都要有错误处理

---

**计划制定时间**：2025-11-19
**预计完成时间**：2-3天（按每天8小时工作）
**当前状态**：✅ 计划已完成，等待执行
